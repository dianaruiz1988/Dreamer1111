// const express = require('express');
// const router = express.Router();
// const diaryCtrl = require('../../controllers/api/diary');

// // GET /api/goal
// router.get('/', diaryCtrl.index);
// // GET /api/goal/:id
// router.get('/:id', diaryCtrl.show);

// module.exports = router;

//when someone makes a request to items its going to call the index

//when someone makes a request to items/id it will show 